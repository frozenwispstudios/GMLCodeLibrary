/// scr_smooth_value_gain(player_exp);

/// @param player_exp

//set arugments
player_exp = argument0;
exp_display = 0;

//human_exp = to player value exp
if exp_display < player_exp{
    exp_display +=0.1;
}